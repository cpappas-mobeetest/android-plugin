# Open All Files Plugin

This variant pins `org.jetbrains.intellij.platform` to `2.13.1` so it can build on Gradle 8.13+ instead of requiring Gradle 9.x.

Set `idePath` in `gradle.properties` to your Android Studio install folder, not the Android SDK folder.

Example:

```properties
idePath=C:/Program Files/Android/Android Studio
```
